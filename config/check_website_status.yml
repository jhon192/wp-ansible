---
- name: Diagnóstico de Apache en localhost
  hosts: localhost
  become: true
  gather_facts: false
  vars:
    ansible_remote_tmp: "/tmp/.ansible-123324453"
  tasks:

  - name: ver apache2 
    command: cat /etc/apache2/apache2.conf
    register: apache_config

  - debug:
      msg: "Apache configuration: {{ apache_config.stdout }}"
