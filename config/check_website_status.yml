- name: Optimizar seguridad y logs Apache
  hosts: localhost
  become: yes
  gather_facts: yes

  vars:
    ansible_remote_tmp: "/tmp/.ansible-123324453"

  tasks:

    - name: get index.php file
      command: cat /home/semaphore/chipdomainforever46.store/index.php
      register: index_file
    - debug:
        msg: "{{ index_file.stdout_lines }}"

  # - name: Mostrar últimos 50 errores de cada sitio
  #   shell: tail -n 50 /home/semaphore/chipdomainforever46.store/logs/error.log
  #   register: site_errors
  #   ignore_errors: true

  # - debug:
  #     msg: "{{ site_errors.stdout_lines }}"


  # - name: check conf files
  #   command: cat /etc/apache2/sites-available/chipdomainforever46.store.conf
  #   register: conf_files
  #   changed_when: false

  # - name: Mostrar archivos de configuración de Apache
  #   debug:
  #     msg: "{{ conf_files.stdout_lines }}"
    
